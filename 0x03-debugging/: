#include "main.h"

/**
 * main - Tests the function 
 *that prints if an integer is positive or n6egative
 *
 * Return: 0
 */
int main(void)
{
        int i;

        i = 0;
        positive_or_negative(i);

        return (0);
}
